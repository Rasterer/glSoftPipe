CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

PROJECT(GLSOFTPIPE)

IF(UNIX)
	SET(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wall -std=gnu++0x -DGL_GLEXT_PROTOTYPES")
ENDIF(UNIX)

ADD_SUBDIRECTORY(src)
ADD_SUBDIRECTORY(unittests)
